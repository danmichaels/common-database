drop table test;
create table test(
	int_value int,
	bigint_value bigint,
	smallint_value smallint,
	varchar_value varchar(200)
);

drop table hiber_test;
create table hiber_test(
	id int not null,
	name varchar(200) not null
);